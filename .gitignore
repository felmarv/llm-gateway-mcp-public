# Secrets — NEVER commit
.env
*.env
!.env.example

# Spend ledger / local data
.llm_gateway/
*.db
*.db-wal
*.db-shm

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# OS / editor
.DS_Store
.idea/
.vscode/

# uv.lock — este repo NO lo versiona (decisión declarada en .github/workflows/tests.yml:12:
# el CI corre SIN `--frozen` porque el lock está ausente en el clon). Ignorarlo mantiene
# cierta esa premisa; una corrida local de `uv` ya no deja WIP huérfano.
uv.lock
