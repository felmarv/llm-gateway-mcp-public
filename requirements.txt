# Core
mcp[cli]>=1.2.0
httpx>=0.27.0
pyyaml>=6.0

# Providers
anthropic>=0.40.0
google-genai>=0.4.0
# OpenAI and DeepSeek are called over httpx (OpenAI-compatible) — no extra SDK.

# Optional convenience
python-dotenv>=1.0.0

# Dev / test
pytest>=8.0.0
